{
  "name": "multiterminal",
  "version": "0.1.0",
  "description": "Grid of real terminals in one window, with template launching and broadcast input.",
  "main": "electron/main.js",
  "private": true,
  "scripts": {
    "start": "electron . --no-sandbox",
    "postinstall": "electron-builder install-app-deps"
  },
  "dependencies": {
    "@xterm/addon-fit": "^0.11.0",
    "@xterm/xterm": "^6.0.0",
    "node-pty": "^1.0.0"
  },
  "devDependencies": {
    "electron": "^31.0.0",
    "electron-builder": "^24.13.3"
  }
}
